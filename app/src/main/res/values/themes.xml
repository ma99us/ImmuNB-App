<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MyHealthNB" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <item name="android:textColor">@color/dark_text</item>
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/header_bg</item>
        <item name="colorPrimaryVariant">@color/body_bg</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/header_bg</item>
        <item name="colorSecondaryVariant">@color/body_bg</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- surface -->
        <item name="colorSurface">@color/body_bg</item>
        <item name="colorOnSurface">@color/black</item>
        <item name="colorOnBackground">@color/dark_text</item>
        <item name="android:colorBackground">@color/body_bg</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/header_bg</item>
        <!-- Customize your theme here. -->
    </style>
</resources>